Perform the following query scenarios on the dvdrental sample database:<br>

- In the film table, the film length is shown in the length column. How many films have a length greater than the average film length?<br>
`SELECT COUNT(*) FROM film
WHERE length > 
(
SELECT AVG(length) FROM film
);`
<img width="1563" height="969" alt="image" src="https://github.com/user-attachments/assets/22eb5470-ae77-4bcb-994b-e9e6d2eb6b11" />

- How many films in the film table have the highest rental_rate value?<br>
`SELECT COUNT(*) FROM film
WHERE rental_rate = 
(
SELECT MAX(rental_rate) FROM film
);`
<img width="1558" height="974" alt="image" src="https://github.com/user-attachments/assets/54a4ddd7-a25f-4fb3-9e91-51a57eeccb7c" />

- List the films in the film table that have the lowest rental_rate and the lowest replacement_cost values.<br>
`SELECT title FROM film
WHERE rental_rate = 
(
SELECT MIN(rental_rate) FROM film
) AND
replacement_cost = 
(
SELECT MIN(replacement_cost) FROM film
);`
<img width="1571" height="972" alt="image" src="https://github.com/user-attachments/assets/8ba82b24-8b0c-46b8-8079-e54ee98613e7" />

- In the payment table, list the customers who have made the highest number of payments.<br>
`SELECT 
    customer.customer_id,
    customer.first_name,
    customer.last_name,
    COUNT(payment.payment_id) AS total_payments
FROM customer 
JOIN payment  ON customer.customer_id = payment.customer_id
GROUP BY customer.customer_id, customer.first_name, customer.last_name
ORDER BY total_payments DESC
LIMIT 1;
`
<img width="1579" height="978" alt="image" src="https://github.com/user-attachments/assets/47a018c0-85d2-46b1-8a2f-2f058bf1bed0" />
